/*class places {
	constructor(x, y, w, h, type) {
		this.x = x;
		this.y = y;
		this.w = 50;
		this.h = 50;
		this.type = type;
	}

	display() {
		rect(this.x, this.y, this.h, this.w);
	}

	Types() {
		var setType;
		let types = [
			"p1",
			"p2",
			"p3",
			"p4",
			"p5",
			"p6",
			"p7",
			"p8",
			"p9",
			"p10",
			"p11",
			"p12",
			"p13",
			"p14",
			"p15",
			"p16",
			"p17",
			"p18",
			"p19",
			"p20",
		];

		// ! getTypes

		if (this.type === types[0]) {
			setType = types[0];
		} else if (this.type === types[1]) {
			setType = types[1];
		} else if (this.type === types[2]) {
			setType = types[2];
		} else if (this.type === types[3]) {
			setType = types[3];
		} else if (this.type === types[4]) {
			setType = types[4];
		} else if (this.type === types[5]) {
			setType = types[5];
		} else if (this.type === types[6]) {
			setType = types[6];
		} else if (this.type === types[7]) {
			setType = types[7];
		} else if (this.type === types[8]) {
			setType = types[8];
		} else if (this.type === types[9]) {
			setType = types[9];
		} else if (this.type === types[10]) {
			setType = types[10];
		} else if (this.type === types[11]) {
			setType = types[11];
		} else if (this.type === types[12]) {
			setType = types[12];
		} else if (this.type === types[13]) {
			setType = types[13];
		} else if (this.type === types[14]) {
			setType = types[14];
		} else if (this.type === types[15]) {
			setType = types[15];
		} else if (this.type === types[16]) {
			setType = types[16];
		} else if (this.type === types[17]) {
			setType = types[17];
		} else if (this.type === types[18]) {
			setType = types[18];
		} else if (this.type === types[19]) {
			setType = types[19];
		} else {
			setType = types[20];
		}

		return setType;
	}


}
*/